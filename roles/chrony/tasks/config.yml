- name: "Create Config Staging Folder"
  file: path=./staging/{{ inventory_hostname }}/chrony/config/ state=directory
- name: "Create Config"
  template: src=./config.j2 dest=./build/{{ inventory_hostname }}/chrony.conf